const a = x => {
  for (let _x of {}) _x;
};

const b = x => {
  for (const _x2 in []) _x2;
};

const c = x => {
  for (let _x3 = 0;;) _x3;
};

const d = () => {
  let x;

  for (let x of {}) x;
};

const e = x => {
  for (let _x4 of {}) {
    let x;
    x;
  }
};
